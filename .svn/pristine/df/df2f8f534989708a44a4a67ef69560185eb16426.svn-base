using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.ComponentModel.Design;
using System.ComponentModel;
using System.Windows.Forms;
using System.Collections;

namespace ApptitudeFormDesigner
{
    class CustomDesignerFilterService : ITypeDescriptorFilterService
    {

       

        #region ITypeDescriptorFilterService Members

        public bool FilterAttributes(System.ComponentModel.IComponent component, System.Collections.IDictionary attributes)
        {
            

            //Form f = new Form();
            //f.Text = component.GetType().ToString();
            //ListBox lb = new ListBox();
            //foreach (DictionaryEntry item in attributes)
            //{

            //    lb.Items.Add((item.Key + " = " + item.Value));

            //}

            //f.Controls.Add(lb);
            //lb.Dock = DockStyle.Fill;
            //f.Show();
            return true;
            
        }

        public bool FilterEvents(System.ComponentModel.IComponent component, System.Collections.IDictionary events)
        {
            return true;
        }

        public bool FilterProperties(System.ComponentModel.IComponent component, System.Collections.IDictionary properties)
        {
            //Form f = new Form();
            //f.Text = component.GetType().ToString();
            //ListBox lb = new ListBox();
            //List<string> keys = new List<string>();
            //MessageBox.Show(component.GetType().ToString());
            //Attribute[] attr = { DesignOnlyAttribute.No };
            //foreach (DictionaryEntry item in properties)
            //{

            //    lb.Items.Add((item.Key + " = " + item.Value));
            //    keys.Add(item.Key.ToString());
                

            //}

            //for (int i = 0; i < keys.Count; i++)
            //{
            //    //properties[keys[i]] = TypeDescriptor.CreateProperty(component.GetType(), keys[i], typeof(int), attr);
            //}

            ////properties["Tag"] = TypeDescriptor.CreateProperty(component.GetType(), "Tag", typeof(int), attr);
            ////properties["DataBindings"] = TypeDescriptor.CreateProperty(component.GetType(), "DataBindings", typeof(int), attr);
            ////properties["Appearance"] = TypeDescriptor.CreateProperty(component.GetType(), "Appearance", typeof(int), attr);
            ////properties["DataBindings"] = TypeDescriptor.CreateProperty(component.GetType(), "Name", typeof(int), attr);
            //f.Controls.Add(lb);
            //lb.Dock = DockStyle.Fill;
            ////f.Show();
            return true;
        }

      

        #endregion
    }
}
